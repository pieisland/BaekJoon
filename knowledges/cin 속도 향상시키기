시간 초과가 계속 나서.. 뭔가 했더니 이것들을 추가하니까 해결된 경우가 몇몇 있었다. 

ios_base::sync_with_stdio(0);
cin.tie(0);

이유는 잘 모르겠다. 대강 검색을 해보니, 
cin이 느려지는 이유는 c 라이브러리의 stdio buffer와 동기화하는 과정에서 느려진다고 한다.
입력이 일어나기 전에 이것을 추가해주면 동기화를 풀고 속도를 끌어올릴 수 있다고.

무슨 말이야 ㅋㅋㅋㅋ...
cin.tie(0)도 찾아봐야되나.
