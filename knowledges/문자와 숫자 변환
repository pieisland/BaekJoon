  일단 문자열을 받을 때 char형 배열을 선언해 s를 해서 받은 후, 
  
  char arr[100000];
  scanf("%s", arr);
	int len = strlen(arr);
  
  문자에서 '0'을 빼주면 숫자로 변환된다.
  
  for (int i = 0; i < len; i++)
  {
		int num = arr[i] - '0';
	}
  
  문자열을 atoi(s)를 쓰면 된다는데, 전체 문자열에서만 되지 하나의 문자에 대해서 바꾸는 게 안되어서.. 패스했음.
